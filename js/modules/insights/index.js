/* Console — modules/insights. Replaced entirely when Phase 7 (Analytics + Insights) is built. */
(function () {
  'use strict';
  window.Console = window.Console || {};
  Console.modules = Console.modules || {};
  Console.modules.insights = Console.lib.createStubModule(
    'Insights',
    'The 10 daily detectors ship in Phase 7.'
  );
})();
