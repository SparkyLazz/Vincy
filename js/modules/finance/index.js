/* Console — modules/finance. Replaced entirely when Phase 5 (Finance) is built. */
(function () {
  'use strict';
  window.Console = window.Console || {};
  Console.modules = Console.modules || {};
  Console.modules.finance = Console.lib.createStubModule(
    'Finance',
    'YNAB-style envelopes, multi-currency, and subscription audit ship in Phase 5.'
  );
})();
