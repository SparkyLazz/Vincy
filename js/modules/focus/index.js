/* Console — modules/focus. Replaced entirely when Phase 6 (Focus Sessions) is built. */
(function () {
  'use strict';
  window.Console = window.Console || {};
  Console.modules = Console.modules || {};
  Console.modules.focus = Console.lib.createStubModule(
    'Focus',
    'Pomodoro/freeform/stopwatch timers and cross-module triggers ship in Phase 6.'
  );
})();
